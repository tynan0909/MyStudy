10장 클래스 변수와 클래스 메소드 - 10-1 : static 선언을 붙여서 선언하는 클래스 변수

인스턴스와 static은 서로 상극이다
static은 공유의 개념이다
클래스 변수는 인스턴스 생성 이전에 메모리공간에 존재한다

static은 로딩할때부터  실행된다


11장 메소드 오버로딩과 String 클래스
11-1 메소드 오버로딩 : 동일한 메소드 이름이 존재할 수 있다

오버로딩의 조건 : 메소드 이름, 타입

초록동그라미 - 엑세스 접근 지식 지시자?

생성자도 오버로딩이 가능함
한 생성자에서 다른 생성자로 가려면 this를 써야 함
또한 메모리에 먼저 올려야 하기에 this를 먼저 올리고, 다른 코드를 실행해야 함.


11-2 String클래스

String은 new를 생략해도 되지만 new를 사용하면
같은글자일때 첫 글자를 참조하지 않고 따로 생긴다

또한 String에 올린건 변경할 수 없다 = String 인스턴스는 Immutable이다
정확히는 String이 참조하는 대상은 불변이고 새 집을 만들어서 그걸 참조하는 것
따라서 String을 만들면 최대한 변경이 안되게 해야 함(메모리를 자꾸 잡아먹으니까)

만약 변경이 필요하면 StringBuilder★(쓰레드X)나 
StringBuffer(쓰레드O_)를 써라

substring은 글자를 추출한다
substring(2)는 2번째부터 끝까지 추출, (2,4)는 2~4가 아니라 2~3이다

compare(cmp)라는 단어가 나오면 비교

쓰레드 Safe

흐름 = 쓰레드 -> 많을 수록 병렬처리

Ctr + Sft + R	or	Ctr + Sft + T
R은 리소스(내가 가지고 있는 소스)
T는 타입(자바에서 제공해주는 소스)

synchronized를 써야 복사를 막을 수 있다?(쓰레드를 세이프할 수 있다 대신 느리다)

