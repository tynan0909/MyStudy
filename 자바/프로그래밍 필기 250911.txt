알면 쓸모있는 자바 개념(시험은안나옴)

열거형
가변인자
어노테이션

네스티드 클래스	|
람다			|
람다 표현식	|	요것들은 하나라고 보면 됨
메소드 참조	|
Optional		|
스트림		|

시각, 날짜
I/O 스트림
NIO
쓰레드


25장 - 열거형(enum)

인터페이스는 static final이 생략되어 있다. 따라서 인터페이스에 정의 된 수는 상수(META DATA)
그래서 new로 올릴 이유 없음
enum으로 선언한것도 상수임. ex) enum Mbti { INTP, ESFJ, ENFP }

25-1 가변인자

. . . <- 이게 가변인자임 가변 : 변형이 가능하다
배열로 들어온다

어노테이션(주석)

reflect -> 관심법(어노테이션 나오면 리플렉션 생각해라)
상품에 태그를 붙여서 작동하게 만드는거?

26장 - 네스~~스트림

클래스 안에 클래스를 만들수 있다.
여기에 스태틱을 붙이려면 클-클-메에도 스태틱 붙이고 클-클에도 스태틱 붙여야함
클-클을 이너클래스(스태틱제외)라고 하고 이거 부르려면 new A().new AAA().aaa(); 이런식으로 불러야함
lazy기법(게으른 기법) - 성능을 위해 딱 필요할때 기능을 불러서 쓰는거(이너클래스 안에 메소드 넣음)

람다 - 이너클래스를 메소드 안에 넣을수도 있다 슥게~~(로컬클래스)
메소드가 발동할때에야 크래스가 정의되고 실행(다만 기능이 딱 하나만 있어야댐)
보통 안드로이드에서 클릭했을때 실행하기 위함

26-2 람다(Lambda)의 소개(사실 네스람다 사이에 익명클래스 있엇는데 이제 안씀)

-> 이게 람다임 리턴타입 메소드명 ( );

람다식 실습)

1. 사칙연산이 가능한 계산기를 람다식으로 구현하시오		힌트: 인터페이스 먼저 설계하고 람다구현


27장 람다 표현식

