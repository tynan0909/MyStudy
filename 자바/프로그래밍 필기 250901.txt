연습

1. 현대자동차의 아이오닉5는 가격이 5000만원 입니다.

2.일본 맛집을 공유하는 클래스를 설계해보세요. public Restaurant[] sns(String ingredient) {}

3. 정보를 변경할 수 있는지 없는지에 대한 참과 거짓을 표현해보세요. - boolean

4. 자판기에 동전을 넣으면 음료수가 나온다.
상품과 잔돈을 둘다 돌려주려면 합쳐야함 >> 클래스 만들어서 다 넣어

5. 생성자 오버로딩 3개이상 + 메소드 오버로딩	> 매개변수가 다르게 여러개 만들면 됨(순서바뀌면 다른거로 취급)
	Overloading(int a){}
	Overloading(int a, int b){}
	Overloading(char a){}
	void Overloading(int a) {}
	void Overloading(int a, int b) {}
	void Overloading(char a) {}

sysout도 오버로딩임(편하게 쓸 수 있다)

6. new 연습) 경찰공무원 6000여명을 채용한다고 합니다.  를 new를 활용해 만들기

7. static 연습) 경찰공무원은 서울시 소속 경찰공무원입니다. 를 static으로, 


상수는 대문자로 표현할 것


13장 배열 

스태틱은 int를 못올리니까 new C().c[5]로 올려야함

ERR: AIOOB(어레이 인덱스 아웃오브바운드) : 인덱스 범위를 넘을 수 없다. -> 돌려보기전엔 빨간줄 안뜸

System.arraycopy( )
Arrays.copyOf( )

얕은 복사 : 원본과 다른 메모리에 존재하지만 내부객체나 배열은 원본과 동일한 참조값 -> 내부데이터가 변경되면 원본과 복사본 모두 영향받음
깊은 복사 : new 두번써서 서로 같은 객체를 참조하지 않게 함.

연습 8. 배열 깊은 복사 연습) 마트에 가서 카트에 물건을 3개 담았어요. 물건 3개는 고등어, 우유, 물입니다. 다른카트에 동일한 물건을 담아보세요.


EnhancedFor 문

for(int e: ar) { }

사망연산자는 ?임


2차원배열

int[ ][ ] arr = new int[3][4];  -> 세로가 3, 가로가 4인 int형 2차원 배열 생성


배열종합문제) 1. 표값 계산하기(Ticket[54300])
에버랜드에 단체 관광객이 티켓을 예매하려고 합니다.
총인원: 54300명
티켓 금액: 어른 8만원, 청소년 5만원, 유아 2만원
어른: 기본값, 청소년: 3의 배수, 유아: 마지막300명(54001~54300)
매 입장시 sysout 출력(예 100번째 손님 - 어른(누적금액)
총합을 구해보세용
검증: 어른/청소년/유아 각각 티켓개수 확인(배열에 따로 담기

